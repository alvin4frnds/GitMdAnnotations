import 'package:flutter/material.dart';

import '../_placeholder.dart';

class ConflictArchivedScreen extends StatelessWidget {
  const ConflictArchivedScreen({super.key});

  @override
  Widget build(BuildContext context) => const MockupPlaceholder(
    title: 'Conflict archived',
    note: 'Modal: remote won; local backed up to app documents dir.',
  );
}
