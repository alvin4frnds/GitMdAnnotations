import 'package:flutter/material.dart';

import '../_placeholder.dart';

class ApprovalConfirmationScreen extends StatelessWidget {
  const ApprovalConfirmationScreen({super.key});

  @override
  Widget build(BuildContext context) => const MockupPlaceholder(
    title: 'Approval confirmation',
    note: 'Modal: create empty 05-approved + changelog.',
  );
}
