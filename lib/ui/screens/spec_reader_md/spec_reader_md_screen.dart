import 'package:flutter/material.dart';

import '../_placeholder.dart';

class SpecReaderMdScreen extends StatelessWidget {
  const SpecReaderMdScreen({super.key});

  @override
  Widget build(BuildContext context) => const MockupPlaceholder(
    title: 'Spec reader (markdown)',
    note: 'Read-only spec render with heading nav.',
  );
}
